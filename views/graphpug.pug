doctype
html
  head
    title ChartistGraph
  script(src='/css/chartist.css')
  script(src='/js/chartist.js')
  body
    header
      h1 My Site
      #mychart
      script.
        console.log(JSON.stringify(#{data}))
        console.log(JSON.stringify(#{options}))
        console.log(JSON.stringify(#{responsiveOptions}))
        new Chartist.Line('#mychart', JSON.stringify(#{data}),JSON.stringify(#{options}), JSON.stringify(#{responsiveOptions}));