doctype
html
  head
    title ChartistGraph
  script(src='/css/chartist.css')
  script(src='/js/chartist.js')
  body
    header
      h1 My Site
      #mychart
      script.
        console.log(#{data})
        console.log(#{options})
        console.log(#{responsiveOptions})
        new Chartist.Line('#mychart', #{data}, #{options}, #{responsiveOptions});
        	